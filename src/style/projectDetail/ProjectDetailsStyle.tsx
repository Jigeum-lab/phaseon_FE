import styled from 'styled-components';

export const Div = styled.div`
  width: 260px;
  height: 100%;
  display: flex;
  justify-content: center;
  position: absolute;
  right: calc(40px + ((100vw - 1200px) / 2));
`;
